const cache=new Map<string,Promise<HTMLImageElement>>();
function image(src:string){return new Promise<HTMLImageElement>((resolve,reject)=>{const im=new Image();im.onload=()=>resolve(im);im.onerror=()=>reject(new Error('主廚造型載入唔到，請再試。'));im.src=src})}
// Matte the generated chroma source during the user's in-browser card composition.
export function keyChefPixels(data:Uint8ClampedArray){for(let i=0;i<data.length;i+=4){const r=data[i],g=data[i+1],b=data[i+2],excess=g-Math.max(r,b);if(g>130&&excess>70){data[i+3]=0}else if(excess>25&&g>85){data[i+3]=Math.round(data[i+3]*(1-(excess-25)/65));data[i+1]=Math.max(r,b)}}return data}
export function loadChef(gender:string,suit:string){const key=gender+'-'+suit;let promise=cache.get(key);if(!promise){promise=(async()=>{const im=await image('/assets/chef-'+key+(suit==='black'?'-chroma':'')+'.png');if(suit!=='black')return im;const canvas=document.createElement('canvas');canvas.width=im.width;canvas.height=im.height;const ctx=canvas.getContext('2d')!;ctx.drawImage(im,0,0);const pixels=ctx.getImageData(0,0,canvas.width,canvas.height);keyChefPixels(pixels.data);ctx.putImageData(pixels,0,0);return image(canvas.toDataURL('image/png'))})();cache.set(key,promise);promise.catch(()=>cache.delete(key))}return promise}
