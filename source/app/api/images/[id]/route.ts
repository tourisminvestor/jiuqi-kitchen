import { bucket,database } from '@/lib/storage';
export async function GET(_:Request,{params}:{params:Promise<{id:string}>}){try{const {id}=await params;if(!/^[0-9a-f-]{36}$/.test(id))return new Response(null,{status:404});const post=await database().prepare('SELECT image_key FROM posts WHERE id = ?').bind(id).first<{image_key:string}>();if(!post)return new Response(null,{status:404});const obj=await bucket().get(post.image_key);if(!obj)return new Response(null,{status:404});return new Response(obj.body,{headers:{'Content-Type':'image/png','Cache-Control':'private, max-age=3600','X-Content-Type-Options':'nosniff'}});}catch(e){console.error(e);return new Response(null,{status:503});}}
